body {
    font-family: 'Arial', sans-serif;
    display: flex;
    justify-content: center;
    align-items: center;
    height: 100vh;
    background-color: #f0f8ff;
    margin: 0;
    overflow: hidden;
    background-image: url('https://example.com/background.jpg'); /* Optional: Replace with a festive background image URL */
    background-size: cover;
}

.container {
    text-align: center;
    background-color: rgba(255, 255, 255, 0.8);
    padding: 40px;
    border-radius: 15px;
    box-shadow: 0px 10px 20px rgba(0, 0, 0, 0.2);
}

h1 {
    font-size: 3rem;
    color: #ff007f;
    margin-bottom: 20px;
}

p {
    font-size: 1.5rem;
    color: #333;
    margin-bottom: 30px;
}

button {
    padding: 15px 30px;
    background-color: #ff007f;
    color: white;
    border: none;
    border-radius: 10px;
    cursor: pointer;
    font-size: 1.2rem;
    box-shadow: 0px 5px 10px rgba(0, 0, 0, 0.1);
    transition: background-color 0.3s ease;
}

button:hover {
    background-color: #e60073;
}

.animation {
    margin-top: 20px;
}
/* Balloon animation */
@keyframes rise {
    0% {
        transform: translateY(100%);
    }
    100% {
        transform: translateY(-100vh);
    }
}

.balloons {
    position: fixed;
    bottom: 0;
    left: 50%;
    transform: translateX(-50%);
    width: 100px;
    height: 200px;
    background-color: #ff007f;
    border-radius: 50%;
    animation: rise 4s infinite ease-in-out;
    opacity: 0.8;
}

.balloons:nth-child(2) {
    background-color: #ffa500;
    animation-delay: 2s;
}
<div class="animation">
    <div class="balloons"></div>
    <div class="balloons"></div>
</div>
