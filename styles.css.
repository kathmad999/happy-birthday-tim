/* Body: Full-screen gradient background */
body {
    font-family: 'Arial', sans-serif;
    display: flex;
    justify-content: center;
    align-items: center;
    height: 100vh;
    margin: 0;
    background: linear-gradient(45deg, #f0f8ff, #ff69b4, #ff1493, #ffa500);
    background-size: 400% 400%;
    animation: gradientShift 10s ease infinite;
    overflow: hidden;
}

/* Gradient animation for background */
@keyframes gradientShift {
    0% { background-position: 0% 50%; }
    50% { background-position: 100% 50%; }
    100% { background-position: 0% 50%; }
}

/* Card Design: This is the birthday card layout */
.card {
    background-color: white;
    padding: 40px;
    width: 90%;
    max-width: 600px;
    border-radius: 15px;
    box-shadow: 0px 10px 20px rgba(0, 0, 0, 0.2);
    text-align: center;
    position: relative;
    border: 5px solid #ff69b4; /* Pink border for decoration */
}

/* Header for the birthday message */
#birthdayMessage {
    font-size: 2.5rem;
    color: #ff007f;
    margin-bottom: 20px;
    font-weight: bold;
}

/* Beautiful longer birthday wish text */
#birthdayWish {
    font-size: 1.5rem;
    color: #555;
    line-height: 1.8;
    margin-bottom: 20px;
    padding: 10px 20px;
    background-color: #fdf7ff; /* Subtle background for text */
    border-radius: 10px;
    box-shadow: 0px 5px 10px rgba(0, 0, 0, 0.1);
}

/* Dancing Emoji inside the card */
#dancingEmoji {
    font-size: 3rem;
    margin-top: 20px;
    animation: pulse 1.5s infinite;
}

/* Play button styling */
button {
    padding: 15px 40px;
    background-color: #ff69b4;
    color: white;
    border: none;
    border-radius: 50px;
    cursor: pointer;
    font-size: 1.5rem;
    font-weight: bold;
    box-shadow: 0px 8px 15px rgba(0, 0, 0, 0.1);
    transition: all 0.3s ease;
    margin-top: 30px;
}

/* Hover effect for the button */
button:hover {
    background-color: #ff1493;
    transform: scale(1.05);
}

/* Pulse animation for emoji */
@keyframes pulse {
    0%, 100% { transform: scale(1); }
    50% { transform: scale(1.1); }
}

/* Balloon
